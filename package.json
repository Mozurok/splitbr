{
  "name": "splitbr-monorepo",
  "private": true,
  "packageManager": "pnpm@11.1.2",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "drift-check": "node scripts/drift-check.mjs",
    "site:dev": "vitepress dev docs/site",
    "site:build": "node scripts/site-sync-readmes.mjs && vitepress build docs/site",
    "site:preview": "vitepress preview docs/site"
  },
  "devDependencies": {
    "vitepress": "1.6.4"
  }
}
